const raio = 6.28
const area = Math.PI * Math.pow(raio, 2)    // Área = 3.14 * raio^2, sendo Math.pow gera exponenciação

console.log(area)           
console.log(typeof(Math))   // Math é um objeto, não uma função